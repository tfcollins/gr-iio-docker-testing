# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'Ubuntu-16.04'

- script: |
    docker build . -t gr-iio.docker.ubuntu16.04  -f gr-iio.docker.ubuntu16.04
    docker run -d gr-iio.docker.ubuntu16.04
